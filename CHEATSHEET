SETUP (Once)
git config --global user.name "Your Name"
git config --global user.email "you@email.com"
git clone <repo-url>

📁 DAILY WORKFLOW (90% of usage)
git status              # See what's going on
git add .               # Stage changes
git commit -m "message" # Save snapshot
git pull                # Get team changes
git push                # Share your code


🔑 Rule:

Always git pull before git push

🌿 BRANCHING (Team Work)
git branch                      # List branches
git checkout -b feature-name    # Create & switch
git switch feature-name         # Switch branch
git merge feature-name          # Merge into current


🧠 Rule:

One feature = one branch
No direct push to main

🔀 MERGE CONFLICTS
git pull
git merge branch-name


Conflict markers:

<<<<<<< HEAD
your code
=======
their code
>>>>>>> branch-name


Fix → save → add → commit

🧳 STASH (Hackathon Lifesaver)
git stash
git stash list
git stash pop


Use when:

Code is broken

Need to pull urgently

Switching tasks

⏪ UNDO MISTAKES (SAFE FIRST)
git restore file-name            # Discard file changes
git reset --soft HEAD~1          # Undo commit, keep code
git revert <commit-id>           # Undo commit safely


⚠️ Dangerous:

git reset --hard HEAD~1

🔍 VIEW & DEBUG
git log
git diff
git show <commit-id>

🚨 EMERGENCY COMMANDS
git reflog        # Recover lost commits
git cherry-pick   # Pick a specific commit

🏆 HACKATHON GOLDEN RULES

Pull before push

Commit small & often

Use meaningful messages

Push before sleeping 😴

One person handles final merge

🎯 CORE 12 COMMANDS (MEMORIZE THESE)
git clone
git status
git add
git commit
git pull
git push
git branch
git checkout
git merge
git stash
git reset
git log

